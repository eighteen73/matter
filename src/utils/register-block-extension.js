/**
 * Re-export so extensions can import a public path that ESLint resolves.
 */

// eslint-disable-next-line import/no-unresolved -- package exports map is not resolved by eslint-plugin-import
export { registerBlockExtension } from '@10up/block-components/api/register-block-extension';
